## 结论

这个模型**不是固定向左或固定向右偏**，而是出现了明显的：

> **2 Hz 步态同步摆头 + 横向速度周期振荡，最终形成蛇形轨迹。**

同时还有三个问题在放大偏移：

1. 对角腿承载和落地时序不均衡；
2. 大腿、小腿力矩严重饱和，实际动作被裁剪变形；
3. 当前所谓的 `model_9200_v4` 实际仍是 **v4 零残差初始化状态**，还没有学会真正的纠偏。

---

## 一、CSV数据反映出的核心问题

我对你上传的 `model_9200_v4_30s.csv` 做了统计，数据为：

* 1501帧；
* 控制周期约 `0.02 s`，即50 Hz；
* 总时长约30秒；
* 前进指令始终为 `0.16 m/s`；
* 横移和转向指令均为0。

### 1. 30秒内发生了17次重置

一共形成18个短片段，已结束片段的持续时间：

| 指标     |     结果 |
| ------ | -----: |
| 平均持续时间 | 1.67 s |
| 中位数    | 1.84 s |
| 最短     | 0.20 s |
| 最长     | 3.62 s |

也就是说，它实际上**没有连续稳定行走30秒**，而是每走一两秒就因接触序列异常重置。

17次重置全部发生在“两个对角组同时存在摆动脚”的判定条件下。重置时甚至出现：

* 单脚支撑；
* 同侧两脚支撑；
* 四脚腾空；
* 对角切换尚未完成，另一对角已经抬起。

因此首先不是简单的“路径奖励不够”，而是**步态交接本身没有稳定下来**。

---

### 2. 速度明显过快

去除每次重置后的前0.3秒后：

| 指标        |            数据 |
| --------- | ------------: |
| 指令前进速度    |     0.160 m/s |
| 实际平均前进速度  | **0.247 m/s** |
| 超速比例      |      **约54%** |
| 前进速度均方根误差 |     0.098 m/s |

策略为了获取前进奖励，实际采用了比指令快很多的冲击式步态。

速度越快：

* 支撑腿切换越突然；
* 落地冲击越大；
* 身体偏航角速度越大；
* 电机越容易饱和；
* 对角腿越难同步。

---

### 3. 偏航是典型的步态周期振荡

稳态区间统计：

| 指标        |             数据 |
| --------- | -------------: |
| 横向速度 RMS  |  **0.073 m/s** |
| 偏航角速度 RMS | **1.13 rad/s** |
| 横滚角速度 RMS |     1.20 rad/s |
| 俯仰角速度 RMS |     0.62 rad/s |

进一步按步态相位分析：

* 偏航角速度的一阶周期振幅约为 **1.53 rad/s**；
* 步态相位可以解释约 **90.2%** 的偏航角速度变化；
* 每个周期内，偏航角速度大致从 `+1.5 rad/s` 摆到 `-1.5 rad/s`；
* 横向速度也随步态周期在约 `-0.055～+0.068 m/s` 之间变化。

这说明：

> 不是机器人一直朝某个方向转，而是一个对角组发力时朝一边转，另一个对角组发力时又朝另一边转。

视觉上就会表现为左右扭、蛇形前进，累积后自然走不直。

---

### 4. 两个对角组的接触占空比不一致

四脚接触率：

| 脚     |  接触占比 |
| ----- | ----: |
| FL 左前 | 69.7% |
| FR 右前 | 60.0% |
| RL 左后 | 59.2% |
| RR 右后 | 65.0% |

两个对角组平均接触率：

| 对角组     |      接触占比 |
| ------- | --------: |
| FL + RR | **67.4%** |
| FR + RL | **59.6%** |

差了约 **7.8个百分点**。

平均垂直支撑力：

| 对角组     |      合计垂直力 |
| ------- | ---------: |
| FL + RR | **66.4 N** |
| FR + RL | **57.9 N** |

前一个对角组比后一个对角组高约 **14.6%**。

需要注意，左右两侧总支撑力几乎一样：

* 左侧约62.15 N；
* 右侧约62.20 N。

所以这并不是简单的“左边重、右边轻”，而是：

> **两个对角组的支撑时间和载荷交接不对称。**

并且在具体对角组内部：

* FL+RR支撑时，RR经常比FL承担更多；
* FR+RL支撑时，RL经常比FR承担更多。

这种交替的前后脚力差，正好会制造周期性偏航力矩。

---

### 5. 力矩饱和非常严重

原始PD力矩超过实际限幅的比例：

| 关节   |      饱和比例 |
| ---- | --------: |
| FL大腿 |     19.9% |
| FR大腿 |     22.8% |
| RL大腿 |     28.6% |
| RR大腿 |     24.3% |
| FL小腿 |     30.5% |
| FR小腿 |     36.7% |
| RL小腿 | **40.2%** |
| RR小腿 |     34.6% |

小腿原始请求力矩峰值达到：

* 约67～82 N·m；

但实际允许值只有：

* 约16～17 N·m。

也就是说，策略请求的动作大量被力矩限幅截断。

这会产生一个很重要的后果：

> 策略认为自己下发了某条对称轨迹，但经过每台电机不同的增益、延迟以及力矩裁剪以后，四条腿实际执行出来的轨迹并不对称。

当前电机温度大约只有30～31℃，没有明显热降额，所以当前主要问题不是温度保护，而是**瞬时PD需求过大和硬力矩裁剪**。

---

## 二、为什么代码会产生这种结果

### 1. `model_9200_v4`并不是真正纠偏后的v4模型

当前 `dog_rs01_straight_guarded` 从 `model_9200` 加载基础策略，然后冻结原有actor，只训练一个最大幅度为0.03的小型残差修正头。

关键是：残差网络最后一层的权重和偏置都初始化为0，因此刚加载时：

```text
v4输出 = model_9200原始输出 + 0
```

代码也明确注明这是为了在初始化时与原策略完全一致。

加载普通checkpoint时，原actor会被映射成冻结的 `base_actor`，残差头仍然是刚初始化的零输出。

所以你现在测试的：

```text
model_9200_v4
```

本质上仍然是：

```text
model_9200原模型
```

它只是具备了后续训练纠偏头的结构，并不代表它已经完成了纠偏。

真正应该测试的是继续训练后的：

```text
model_9225
model_9250
model_9275
……
```

---

### 2. 这是纯12关节策略，所有协调都依赖网络自己完成

当前直接12输出任务明确关闭了：

* CPG关节偏置；
* 对角动作投影；
* 摆动小腿参考；
* 支撑腿约束；
* 接触感知相位切换；
* 动作滤波。

步态相位只作为观察和奖励参考，并不直接生成关节轨迹。

这意味着网络必须同时自己学会：

* 两条对角腿同时抬起；
* 两条腿同时落地；
* 落地后完成载荷转移；
* 另一组才能起步；
* 还要控制横滚、偏航和电机差异。

当动作又处于大面积力矩饱和时，这种纯学习协调非常容易被破坏。

---

### 3. 当前奖励仍然更鼓励“往前冲”，而不是严格按速度走直

当前guarded配置中：

```python
tracking_lin_vel = 6.0
normalized_command_tracking = 5.0
commanded_smooth_straight_progress = 10.0

absolute_longitudinal_tracking_error = -2.0
yaw_rate = -0.5
body_angular_velocity = -1.5
straight_path_lateral_displacement = -0.2
```

也就是说：

* 前进收益很强；
* 超速惩罚只有 `-2`；
* 单独yaw rate惩罚只有 `-0.5`；
* 累积横向偏移惩罚只有 `-0.2`。

这与CSV完全吻合：

* 策略把0.16 m/s走成0.247 m/s；
* 为了获得前进收益，接受了大幅摆头、超速和电机饱和。

---

### 4. 存在过于严格且重复的终止逻辑

父类 `FanfanRobot.check_termination()` 已经实现了：

* 非法步态连续计数；
* 训练阶段使用3帧容忍；
* 测试阶段使用配置的2帧容忍；
* 达到连续异常帧数后才重置。

而 `DogRs01Robot` 又在父类逻辑之后，直接增加：

```python
self.reset_buf |= grace & locomotion & rear_pair_air
self.reset_buf |= grace & locomotion & overlapping_diagonal
```

即只要当前单帧出现后腿同时腾空或两个对角组重叠摆动，就立即重置。

但它重写的 `_get_non_diagonal_swing_mask()` 本身就已经把：

* 后腿同时腾空；
* 前腿同时腾空；
* 两个对角组重叠摆动；

全部包含进父类的连续计数逻辑。

所以当前是：

> 父类本来允许连续2～3帧判断，子类却又用单帧立即重置绕过了这个容忍机制。

这就是30秒出现17次重置的重要原因。短片段训练会让策略难以学到长期方向稳定性。

---

### 5. 当前策略看不到绝对横向偏移

当前观察量包括：

* 机身线速度；
* 角速度；
* 重力方向；
* 速度指令；
* 关节角度和速度；
* 上一次动作；
* 步态相位；
* 航向误差；
* 电机力矩与温度。

但没有加入：

```text
相对目标直线的横向位移 y_error
```

guarded配置也明确说明，修正头观察的是瞬时横向速度、yaw rate、步态相位和航向误差，并不以绝对横向位置作为主要目标。

因此它可以学习：

* 不要继续向侧面滑；
* 不要继续旋转；
* 保持航向。

但如果已经偏离原始直线10 cm，它并不知道自己偏了10 cm，也就无法主动回到原路线。

---

## 三、建议按这个顺序修改

### 第一步：先修正重复终止逻辑

把 `dog_env.py` 中的：

```python
def check_termination(self):
    super().check_termination()
    locomotion = self._stand_command_gate() < 0.5
    grace = ...
    ...
```

先改成：

```python
def check_termination(self):
    super().check_termination()
```

因为父类调用：

```python
self._get_non_diagonal_swing_mask()
```

时，仍然会调用 `DogRs01Robot` 重写后的严格版本，所以后腿同抬、前腿同抬、对角重叠仍然会被检测，只是不再因为单帧接触抖动立即重置。

训练初期建议：

```python
non_diagonal_swing_termination_steps = 4
non_diagonal_swing_termination_steps_test = 3
```

对应50 Hz下：

* 训练连续异常80 ms才重置；
* 测试连续异常60 ms才重置。

后续步态稳定以后再收紧到2帧。

---

### 第二步：不要把9200当成v4纠偏结果

继续训练：

```text
dog_rs01_straight_guarded
```

然后重点比较：

```text
9225、9250、9275、9300……
```

不是继续反复测试9200。

候选模型至少达到：

| 指标           |          建议门槛 |
| ------------ | ------------: |
| 连续测试时间       |       30 s无重置 |
| yaw rate RMS |  < 0.40 rad/s |
| 横向速度 RMS     |    < 0.03 m/s |
| 30秒末航向误差     |          < 5° |
| 最大横向偏移       |      < 0.05 m |
| 小腿力矩饱和率      |     < 10%～15% |
| 平均前进速度       | 0.15～0.18 m/s |

当前模型距离这些门槛还比较远，尤其是yaw rate和小腿饱和率。

---

### 第三步：重新平衡奖励

建议先做一版保守调整：

```python
class scales:
    tracking_lin_vel = 10.0
    normalized_command_tracking = 8.0
    absolute_longitudinal_tracking_error = -10.0
    commanded_smooth_straight_progress = 5.0

    straight_heading_recovery_rate = -10.0
    straight_path_lateral_velocity = -12.0
    straight_lateral_speed = -5.0
    straight_heading_error = -6.0

    yaw_rate = -1.5
    body_angular_velocity = -3.0
    body_angular_acceleration = -0.8

    straight_contact_lateral_force = -4.0
    straight_contact_yaw_moment = -5.0

    action_rate = -0.08
    torque_clip = -1.2
    motor_thermal_overload = -0.5
```

核心不是无限增大“走直惩罚”，而是：

* 降低单纯前进收益；
* 明显提高速度误差；
* 抑制2 Hz偏航振荡；
* 减少力矩饱和；
* 保留对角步态结构奖励。

训练速度范围也建议暂时限制为：

```python
lin_vel_x = [0.12, 0.16]
```

等模型能在0.16 m/s下不超速、不摆头，再逐步扩展到0.18或0.20 m/s。

---

### 第四步：真正解决“偏离路线后回不来”

在仿真观察量里增加：

```python
lateral_displacement
```

也就是 `_straight_path_state()` 输出的相对目标直线横向位移。该函数目前已经正确计算了世界坐标下的横向偏移、横向速度和航向误差。

然后把它加入：

* `obs_buf`；
* `num_observations`；
* `residual_feature_indices`。

同时重新启用：

```python
straight_path_recovery_velocity = -6.0
straight_path_lateral_displacement = -3.0
```

代码中已经有“根据横向位置误差生成回归横向速度”的奖励实现，它的设计目的就是避免“既惩罚偏移又惩罚纠偏横向速度”的矛盾。

真机上则需要：

* 激光雷达定位；
* 视觉里程计；
* 轮廓/路线跟踪；
* 或其他外部位置估计。

只靠IMU可以压制摆头并维持航向，但无法知道机器人已经横向偏离原路线多少。

---

## 最终判断

目前走不直的原因优先级是：

1. **对角腿交接不稳定，30秒重置17次；**
2. **偏航与2 Hz步态高度同步，属于周期性摆头；**
3. **小腿和大腿大量力矩饱和，导致理想动作执行失真；**
4. **实际速度比指令高54%，策略仍偏向向前冲；**
5. **9200只是v4零残差起点，不是纠偏后的模型；**
6. **观察量缺少横向位置误差，无法真正回到目标直线。**

所以现在不建议单独调一个“左右髋关节补偿值”。数据表明左右总载荷几乎平衡，固定补偿很可能只是把蛇形偏移变成固定向另一侧偏。应先修复终止逻辑、降低力矩饱和和周期性yaw，再训练v4后续checkpoint。
 