# V19：修正落脚偏置和内收（长训由用户启动）

## 工作区与基线

基于HEAD 3a0a7f4、fix/rs01-heading-odom-soft-inhibit及已有V18未提交文件；保留用户修改和旧任务/模型。
来源为A/12000：`logs/rs01_omni_v18_balance18/Sep12_22-22-32_v18_A_clearance18_seed16_20260912_222228/model_12000.pt`。
上轮完整诊断见`../artifacts/rs01_v18_selection/POSTURE_FINDINGS.md`：前腿持续右前伸约108/66/73 mm，四髋内收9–11°，动作截断约74–84%。不是某腿卡机械限位。

## 物理和执行器边界

重新解析实际URDF质量11.7317368323 kg；180 mm大腿、202.158 mm小腿至足心、16 mm足半径均沿用已校验几何。
URDF SHA256仍为48b177e9977cc3644dd7a84433d82c3b6d9293e9fdc40c33daa35b4194dd11f4。
默认足宽294.5 mm只是参考，不强制恢复该宽度。RS01识别FOPDT/延迟/摩擦、Kp40/Kd1、动作幅度/目标速度/加速度限制、14 Nm运行上限及动态降额全部不变；17 Nm仍是硬件峰值语义。
零速度下0.05/0.10/0.20 rad误差的原始PD需求仍为2/4/8 Nm。无依据修改PD不能解决策略目标本身的20°偏置，故本轮不做PD试验。
策略20 ms、物理PD反馈2.5 ms、识别响应5 ms、热历史跨重置不变；61观测/12动作与模型兼容。

## 问题—修改—验收

| 问题 | 修改 | 验收 |
|---|---|---|
| 周期平均前后落脚中心长期错位 | 前/后足对x差以0.6 s低通，15 mm死区、30 mm尺度Huber代价 | 实测完整周期足x中心差；不以每帧镜像验收 |
| 内收且足宽软约束长期不满足 | **替换**旧乘法footprint，独立扣除宽度不足代价；直行/踏步/后退最低参考220 mm，全向140 mm | 足宽、髋内收、支撑与跟踪联合检查 |
| 原始动作长期截断 | 原action_saturation权重-.1→-.5，不增加第二个截断项 | 原始动作超±1比例与速度/稳定性一起看 |

几何代价在原phase_two_contact_quality内扣除，不新增reward名称。
旧footprint乘子被替换为1，不同时保留旧宽度惩罚；支撑本身的奖励梯度不因几何偏差被乘没。
V18身体roll项保持原强度，不继续加码；核心tracking=14及实现不变，orientation仍0；没有动作镜像、等载荷、每帧两脚位置相同的要求。
几何项强度最初A=1、B=.5，两者截断项相同。

## 条件与历史量

raw vy/wz分别达到.08 m/s/.20 rad/s时，前后偏置先验连续释放；不会惩罚heading控制器的纠偏速度。
后退的前后偏置权重为直行的一半，但足宽最低参考不因后退而放松。
使用低通估计，不声称它是精确周期平均；.6 s周期、瞬时90 mm正常反相摆动经滤波残差<15 mm，单测覆盖。
换原始指令及episode重置清空滤波并以.6 s逐渐启用；不把旧指令偏置带入新动作。历史量仅用于训练reward，不新增真机观测或控制状态。
220 mm和滤波参数是软工程先验，不是已验证的物理最优值。

## 文件

- `legged_gym/envs/rs01_omni_v2/rs01_omni_v19_config.py` / `_env.py`：独立配置及reward替换。
- `tests/test_rs01_omni_v19.py`：控制合同、单变量、死区/全向释放、正常交替、重置/切换、旧乘子移除。
- 注册器、全向固定评测和5秒切换脚本支持V19；既有逐腿诊断补充实际几何扣分与tracking贡献。
- `legged_gym/scripts/train_rs01_v19_dual.sh`：需用户显式传`--start`，两卡后台训练并实时显示日志；本轮不执行该启动命令。

## 验证与初步结果

27项测试通过，脚本语法和diff检查通过。两个初始方案均从12000做512环境×100轮到12100，固定lr=1e-4、不加载旧optimizer。
初始A强几何方案在10秒×4环境的右移.30工况发生1次重置和观测腾空，淘汰其长训建议；其后续序列主动停止，不标记为通过。
温和B/seed16通过23运动工况×4环境×10秒：0重置、无观测腾空、数值有限、最大速度17.61 rad/s、原始PD峰值16.58 Nm。

同协议诊断对比：seed20260918、踏步/前进.20/后退-.20各4环境，统计6–12秒。基线从上轮保存的6–30秒NPZ截取首300帧，不混用整段均值：

| 指标：踏步/前进/后退 | A/12000基线 | V19温和/seed16/12100 |
|---|---|---|
| 右前脚领先mm | 108.2/65.1/72.7 | 97.3/62.6/58.2 |
| 前足宽mm | 187.5/190.1/184.3 | 194.7/194.7/191.6 |
| 四髋平均原始动作截断比例 | 79.3/77.4/79.0% | 约72.4/73.7/70.0% |
| 新几何代价实际单步扣分 | 不适用 | .0226/.0126/.0082 |
| tracking实际单步收益 | 本表未提取 | .2483/.2288/.1912 |

**这是方向性改善，不是修好。**踏步仍有近10 cm错位，不能把100轮模型当成合格步态或替代原基线。

## 长训安排（手动）

不推荐失败的强配置；脚本准备温和配置两个种子16/17，均从原A/12000开始、4096环境、各新增3000轮到15000，每50轮保存。
第二种子冒烟和切换结果见后续验证记录；不要把seed16通过等同于所有种子均通过。

最终验证完成：温和配置seed16/17各512环境×100轮，均实际保存12100、固定optimizer lr=1e-4、参数有限。
两者23运动工况×4环境×10秒均0重置、无50 Hz观测腾空。seed17最大关节速度20.16 rad/s、原始PD峰值15.05 Nm。
两者每5秒换动作的125秒×4环境序列均0重置、数值有限，最大关节速度分别16.96/16.46 rad/s。
完整日志已归档至`../artifacts/rs01_v19_placement/`，序列结果为seed16_sequence.json、seed17_sequence.json。
确认本轮**没有启动长训练**；只完成3次100轮冒烟（含被淘汰的强方案）及评测。

```bash
cd /home/nszb/gym/unitree_rl_gym
bash legged_gym/scripts/train_rs01_v19_dual.sh --start
```

脚本会打印PID和日志路径，自动显示两卡实时日志；Ctrl+C仅退出日志查看。不要重复启动；保持电脑开机且不休眠。
若日志中的ETA为负，这是既有resume计数显示问题；看实际iteration从12000递增至15000，不据此判断训练已失败。

## 风险与回退

短测不代替60秒、多种子、长时热/事件级步态验收。最终优先评价实际落脚偏置、内收和截断，再看支撑/弹跳/速度；不只挑reward最高或最后模型。
没有改变MuJoCo/ONNX/真机执行代码，也没有完成新模型Sim2Real验证。
回退直接用V18任务与原A/12000，不需删除文件。所有本轮新模型均放新日志目录；用户原修改未覆盖。
